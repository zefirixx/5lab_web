# Лабораторная работа №5: Работа с базой данных MySQL через PHP и Docker

## Автор
ФИО: Гнездилова Ксения Дмитриевна  
Группа: 2ПМ-ИП-2
Вариант: 4
---

## Описание задания
1. Научиться работать с базой данных MySQL через PHP.
2. Создать таблицу для данных формы.
3. Сохранять данные формы в базу данных.
4. Выводить данные из базы на странице.
5. Использовать классы PHP для работы с таблицей.
6. Работать с Docker контейнерами: nginx (уже есть), PHP-FPM, MySQL, Adminer.

---

## Как запустить проект

1. Клонировать репозиторий:
   ```bash
   git clone <ссылка на репозиторий>
   cd lab
Запустить контейнеры:
```bash
docker-compose up -d --build
```
Открыть в браузере:
```http://localhost:8080```

Содержимое проекта:

```docker-compose.yml``` — описание сервиса Nginx и PHP-FPM

```Dockerfile``` — сборка контейнера PHP с установленным Composer и расширениями

```www/form.html``` — главная HTML-страница c формой регистрации

```www/index.php``` — страница для вывода данных из сессии и данных из API

```www/process.php``` — обработка данных формы, сохранение в сессию и получение данных из API

```www/view.php``` — страница для просмотра всех сохранённых записей из data.txt

```www/data.txt``` — файл для хранения данных

```www/ApiClient.php``` — класс для работы с публичным API (Guzzle)

```www/UserInfo.php``` — класс для получения информации о пользователе

```www/db.php``` — файл подключения к базе данных (если используется)

```composer.json``` — файл конфигурации Composer с зависимостями проекта

```composer.lock``` — файл фиксации версий установленных зависимостей

```vendor/``` — папка с установленными библиотеками Composer (включая Guzzle)

```default.conf``` — конфиг c добавленной секцией для обработки PHP

Результат:
Была настроена среда Docker с PHP и MySQL, создана таблица для формы регистрации, реализован класс PHP для работы с базой, данные участников успешно сохраняются и выводятся на страницу.






